var expect    = require("chai").expect;

describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});

describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});


describe('Suite one', function(){
  it("equals", function () {
      var p = [1,2,3];
        expect(1).to.equal(1);
        expect(2).to.equal(2);
  });
});



